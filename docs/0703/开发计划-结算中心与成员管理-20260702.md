# 开发计划 · 结算中心完善 + 成员管理（20260702）

> 依据：`page-figma-design.md`（8 张设计稿）+ 3 份后端契约（成员与账号 / 收款管理 / 收益明细字段补充）。
> 要求：**1:1 还原设计稿**。本文档含「开发计划」与「给开发 AI 的提示词」两部分，最终由发起人负责 code review 与验收。
> 设计稿参考截图已下载至 `.design-refs/`（8 张 PNG，可直接对照）。

---

## 一、现状盘点（已核实代码）

| 模块 | 现状 | 差距 |
|---|---|---|
| 收款管理 `PaymentPage.tsx` | 已对接 `/publisher/payout/account`，按旧契约实现 | ① 新契约 GET 恒返回对象（`bound` 字段），前端仍按 `null` 判空；② `companyName` 应服务端带入、**只读**，现在是可编辑选填；③ 表单多了「户名」输入（新契约不采集）；④ 空态需按 figma 15150-30625 1:1 核对 |
| 收益明细 `EarningsPage.tsx` | 已对接 overview/earnings/courses，全量拉取+前端过滤 | ① 缺「播放量」「结算状态」两列（`viewCount`/`settleStatus`）；② 缺「结算账单数」`settlementBillCount`；③ `monthlyTrend` 已改为全年 12 项、前端 `normalizeTrend`（补 6 月逻辑）应删除直渲染；④ 搜索应走服务端 `keyword` 入参、按月切换走 `startDate/endDate` |
| 成员管理 | **零代码**（无路由、无 service、无页面） | 全新开发：列表 + 角色权限说明 + 添加账号弹窗（4 态）+ 移除确认弹窗，对接 `/publisher/member/*` |
| 侧边栏 `DistributionLayout.tsx` | 只有 数据概览/上剧/结算中心 三组 | 按设计稿新增「成员管理」入口（`Users` 图标，位于结算中心之后） |

**技术栈约定**（新代码必须遵循）：Vite + React 18 + react-router 7 + Tailwind 4；请求走 `src/app/services/http.ts`（自动带 `token` 头、自动 toast 后端 `msg`、401 跳登录）；toast 用 sonner；i18n 走 `useI18n()`，四语言 **zh-CN / zh-TW / en / pt** 全部要补；页面风格参照现有 distribution 页面（`#101828` 主文字、`#111111` 黑色主按钮、`rounded-2xl border border-[#f3f4f6]` 卡片）。

---

## 二、任务拆分与顺序

### 任务 1（独立）：成员管理模块（全新）— 工作量最大

设计稿 6 张：列表 15149-28384 / 添加弹窗 15149-28629 / 下拉 15149-28908 / 输入 15149-29195 / 校验错误 15149-29474 / 添加成功 15149-30043 / 移除确认 15149-29755。

交付物：
1. `src/app/services/member.ts` — 封装 `GET /publisher/member/list`、`POST /publisher/member/add`、`POST /publisher/member/updateRole`、`POST /publisher/member/remove`、`GET /publisher/member/roles`（类型按契约§1）。
2. `src/app/distribution/pages/MembersPage.tsx` — 页面（结构见下）。
3. 路由 `/distribution/members` + 侧边栏「成员管理」入口。
4. `src/app/distribution/i18n/members.ts` 四语言文案，注册进 `i18n.distribution.ts`。

页面结构（1:1）：
- 页头：左「成员管理」标题，右黑色圆角按钮「添加账号」。
- 蓝色信息条（浅蓝底 `ℹ️` + 文案「管理员已拥有账户下全部剧目的查看权和管理权，无需对管理员进行剧目分配、查看操作。」+ 右侧 `×` 可关闭，关闭态存 localStorage）。
- 成员表格卡片：列 = 成员信息（头像+昵称+本人加「(我)」灰标签，下行「手机: 189****7627」）/ 账号状态（绿点+「使用中」）/ 成员类型（角色徽标：超管橙、管理员蓝、员工灰）/ 操作（「移除」橙色链接；`removable=false` 时灰色禁用）。
- 「角色权限说明」区：3 张卡片横排（数据来自 `/member/roles`），每张 = 角色徽标 + 描述段落 + 权限列表（`allowed=true` 项前橙/蓝色圆点，`allowed=false` 项灰色 `×` 前缀 + 灰字）。
- 添加账号弹窗：标题「添加账号」+ `×`；「* 成员类型」下拉（选项取 roles 中 `addable=true`，默认「员工」，展开态选中项右侧 ✓）；「* 账号手机号」输入框 placeholder「请输入手机号」；底部「取消」白 /「确定」黑。**前端校验：非 11 位数字时输入框红边 + 下方红字「请输入正确的11位手机号」**。成功：关弹窗 + toast + 刷新列表。后端错误码 403328/403329/403330/403331 由 http 层 toast，无需前端映射。
- 移除确认弹窗：标题「是否移除当前成员？」+ `×`；说明文案「移除后，当前成员账号将不再属于此账户，将无权限管理并查看账户内剧目、收益等，移除后无法恢复，请是否移除当前成员？」；灰底成员卡（头像+昵称+「管理员|198****5780」）；「取消」白 /「确认移除」黑。成功后刷新列表。

范围外（本期不做，已确认无设计稿）：剧目分配 UI（契约§2）、账号信息页面（契约§3，侧边栏暂不加「账号信息」入口）、updateRole 的 UI 入口（设计稿列表无改角色交互，仅封装 service 备用）。

### 任务 2（独立，与任务 1 并行）：结算中心适配新契约

**2a 收款管理**（设计稿 15150-30625 空态）：
- `settlement.ts`：`PayoutAccount` 增加 `bound: boolean`，`accountNo/bank/branch/currency/status` 未绑定时可为 null；`getPayoutAccount` 恒返回对象；`PayoutAccountBody` 删除 `companyName`（后端忽略）。
- `PaymentPage.tsx`：判空从 `account === null` 改为 `!account.bound`；表单「公司名称」改为**只读 disabled**、值取接口 `companyName`（自动带入，不随表单提交）；删除「户名 accountHolder」输入项；表单字段仅 银行账号* / 开户银行* / 支行名称(选填)；空态视觉按 15150-30625 逐像素核对。
- `EarningsPage.tsx` 的 `hasAccount` 判断同步改为 `acc.bound`。

**2b 收益明细字段补充**（无新设计稿链接，按契约 + 现有页面风格延展）：
- `settlement.ts`：`EarningsSummary` 增加 `settlementBillCount: number`、`monthlyTrend` 注释改为「本年 1—12 月满 12 项」；`EarningsDetailRow` 增加 `viewCount: number`、`settleStatus: 0|1|2`；`EarningsDetailQuery` 增加 `keyword?: string`。
- `EarningsPage.tsx`：
  - 总览区新增「结算账单」统计（值 = `settlementBillCount`）。
  - 删除 `normalizeTrend`，12 项直接渲染。
  - 明细表新增「播放量」列（千分位/万缩写按现有 formatCount 风格）与「结算状态」列（0→待结算 灰、1→结算中 蓝、2→已结算 绿，徽标样式沿用 `TypeBadge` 风格）。
  - 搜索框改为服务端 `keyword`（防抖 300ms 重新请求）；按月 Tab 切换传该月 `startDate/endDate`；由「全量拉取+前端过滤」重构为服务端分页/筛选。
  - `growthRate === null` 展示 "--"（已有，保留）。
- i18n 四语言补新文案（播放量/结算状态/待结算/结算中/已结算/结算账单）。

### 任务 3：Review 与验收（发起人执行，见 §四）

依赖关系：任务 1 与任务 2 无文件交集（除 `DistributionLayout.tsx` 侧边栏 —— 归任务 1 改），可并行给两个 AI。

---

## 三、给开发 AI 的提示词

### 提示词 A：成员管理模块

```text
你在 /Users/weipeng/Desktop/dev/x-short-video/lollipopai-website 项目中开发「发行中心 · 成员管理」全新模块，要求 1:1 还原 Figma 设计稿。

【技术栈与硬性约定】
- Vite + React 18 + react-router 7 + Tailwind CSS 4，包管理 pnpm，不得新增任何依赖。
- 请求必须走 src/app/services/http.ts 的 http.get/post（自动带 publisher token 头、业务失败自动 toast 后端 msg、401 自动跳登录）。禁止自建 fetch。
- toast 用 sonner（import { toast } from "sonner"）。
- i18n：新建 src/app/distribution/i18n/members.ts，按现有模块（如 i18n/payment.ts）的写法提供 zh-CN / zh-TW / en / pt 四语言，并在 src/app/distribution/i18n.distribution.ts 注册为 messages.distribution.members。所有 UI 文案一律走 i18n，禁止硬编码中文。
- 视觉规范参照现有页面（如 pages/PaymentPage.tsx）：主文字 #101828、次要 #6a7282/#99a1af、黑色主按钮 #111111 rounded-xl、卡片 bg-white rounded-2xl border border-[#f3f4f6]、字重用 style={{ fontWeight: 600 }} 形式。图标用 lucide-react。

【设计稿（1:1 还原，本地截图在 .design-refs/ 目录）】
- 成员列表: .design-refs/member-list.png（figma node 15149-28384）
- 添加账号弹窗: .design-refs/member-add.png（15149-28629）
- 弹窗下拉展开: .design-refs/member-add-dropdown.png（15149-28908）
- 输入手机号: .design-refs/member-add-input.png（15149-29195）
- 手机号不满11位错误态: .design-refs/member-add-invalid.png（15149-29474）
- 添加成功后列表: .design-refs/member-add-success.png（15149-30043）
- 移除确认弹窗: .design-refs/member-remove.png（15149-29755）
Figma 文件: https://www.figma.com/design/IqBKUlC7dZwhHX0hLpuCET/%E7%9F%AD%E5%89%A7app25 （node id 见上）
开发时逐张对照截图核对布局、间距、圆角、配色、文案。

【后端契约】（详见项目根目录 发行中心-成员与账号-前端对接.md §1）
统一响应 {code,msg,data}，code=0 成功。接口（仅超管可用，无权返回 code=500 msg已翻译）：
1. GET /publisher/member/list → data 数组：{ memberUserId, nickname, avatar, phoneMask, role(1超管/2管理员/3员工), roleName, status(1使用中/0停用), statusName, self(boolean), removable(boolean), joinTime }。超管置顶（服务端已排）。
2. POST /publisher/member/add → body { phone, role }，role 仅 2|3。错误码 403328 手机号未注册 / 403329 已是本账户成员 / 403330 已属其他发行账户 / 403331 角色非法（http层自动 toast，前端无需映射文案）。
3. POST /publisher/member/remove → body { memberUserId }。
4. POST /publisher/member/updateRole → body { memberUserId, role(2|3) }（仅封装 service，本期无 UI 入口）。
5. GET /publisher/member/roles → data: [{ code, name, description, addable, permissions:[{ key, name, allowed }] }]，共 3 张角色卡。驱动「角色权限说明」区与添加弹窗的成员类型下拉（下拉只取 addable=true 的项）。

【交付文件】
1. src/app/services/member.ts —— 5 个接口封装 + TypeScript 类型（参照 src/app/services/settlement.ts 的注释风格）。
2. src/app/distribution/pages/MembersPage.tsx —— 页面主体。
3. src/app/distribution/routes.tsx —— 在 DistributionLayout 下新增 <Route path="members" element={<MembersPage />} />。
4. src/app/distribution/DistributionLayout.tsx —— SidebarNav 中「结算中心」之后新增「成员管理」入口（lucide Users 图标，i18n key nav.members，写法照抄现有 SideRow）。nav 的 i18n 在 src/app/distribution/i18n/nav.ts 补四语言。
5. src/app/distribution/i18n/members.ts + i18n.distribution.ts 注册。

【页面行为细节】
- 页头：标题「成员管理」，右侧黑色按钮「添加账号」。
- 蓝色提示条：浅蓝底、info 图标、文案见设计稿、右侧 × 关闭；关闭状态写 localStorage（key 自定，如 lollipop-members-banner-dismissed），刷新不再出现。
- 成员表格：四列 成员信息 / 账号状态 / 成员类型 / 操作。成员信息 = 40px 圆形头像（avatar 空时用灰色占位图标）+ 昵称（self=true 时昵称后加灰色小标签「(我)」）+ 下行小字「手机: {phoneMask}」。账号状态 = 绿色圆点 + statusName（status=0 时灰点）。成员类型 = 徽标：超管橙色系、管理员蓝色系、员工灰色系（浅底深字圆角小标签）。操作 = 「移除」：removable=true 时橙色可点，false 时灰色不可点。
- 角色权限说明：标题下 3 卡横排（grid grid-cols-3 gap-4，窄屏可换行），卡内 = 角色徽标 + description 段落 + permissions 列表：allowed=true 项前带彩色圆点（颜色随角色），allowed=false 项前带灰色 × 且整行灰字。数据全部来自 /member/roles，禁止硬编码权限文案。
- 添加账号弹窗（自建 fixed overlay + 白色圆角卡，参照 DistributionLayout 内已有弹窗写法）：
  - 「* 成员类型」自定义下拉：默认选中「员工」，点击展开选项列表（浮层、圆角、阴影），选中项右侧 ✓，样式对照 member-add-dropdown.png。
  - 「* 账号手机号」输入框，placeholder「请输入手机号」。
  - 点「确定」前端先校验：值必须为 11 位纯数字，否则输入框红边框 + 下方红色小字「请输入正确的11位手机号」（i18n），不发请求。
  - 通过则调 /member/add，成功：关闭弹窗 + toast 成功文案 + 重新拉取列表；失败：http 层已 toast，弹窗保持打开。
  - 提交中按钮禁用 + Loader2 转圈（参照 PaymentPage 的 saving 处理）。
- 移除确认弹窗：标题「是否移除当前成员？」+ ×，说明文案见设计稿，中部灰底圆角成员卡（头像+昵称+「{roleName}|{phoneMask}」），按钮「取消」白 /「确认移除」黑。确认调 /member/remove，成功关弹窗 + toast + 刷新列表。
- 页面加载态复用 src/app/distribution/components/settlement/PageHeader.tsx 的 PageLoading。
- 列表接口报错（如非超管 403335）：http 层已 toast，页面展示空态即可，不做白屏。

【完成标准】
- pnpm build 通过、无新增 lint 错误。
- 四语言文案齐全（zh-CN/zh-TW/en/pt）。
- 7 张设计稿状态全部可复现：列表、弹窗默认、下拉展开、输入中、11位校验错误、添加成功刷新、移除确认。
- 不改动本任务范围外的任何文件。
```

### 提示词 B：结算中心适配新契约（收款管理 + 收益明细）

```text
你在 /Users/weipeng/Desktop/dev/x-short-video/lollipopai-website 项目中，按两份新后端契约改造「发行中心 · 结算中心」的收款管理与收益明细页面。契约原文在项目根目录：发行中心-收款管理-前端对接.md、发行中心-收益明细-字段补充-20260701.md，动手前先通读。

【技术栈与硬性约定】
- Vite + React 18 + Tailwind 4，pnpm，不得新增依赖。请求走 src/app/services/http.ts；toast 用 sonner；i18n 四语言 zh-CN/zh-TW/en/pt，新增文案加到 src/app/distribution/i18n/payment.ts 与 earnings.ts；禁止硬编码中文。
- 涉及文件：src/app/services/settlement.ts、src/app/distribution/pages/PaymentPage.tsx、src/app/distribution/pages/EarningsPage.tsx，及上述两个 i18n 文件。不改其他文件。

【第一部分：收款管理（契约=收款管理-前端对接.md）】
接口变化：GET /publisher/payout/account 现在**未绑定也返回对象**：{ bound:false, companyName:"…"(恒按入驻资料回填), accountNo:null, bank:null, branch:null, currency:null, status:null, … }；bound=true 时为完整账号。POST /publisher/payout/account（新增，已存在报 403315）与 POST /publisher/payout/account/update（修改，不存在报 403316）请求体：{ accountNo(必填≤64), bank(必填≤128), branch(选填≤255), accountHolder(选填,本表单不采集), currency(选填,缺省USD) }。companyName 前端不传，传了后端也忽略。

改造点：
1. settlement.ts：PayoutAccount 增加 bound: boolean；accountNo/bank/branch/currency/status 类型改为可空；getPayoutAccount 返回 Promise<PayoutAccount>（不再 | null）；PayoutAccountBody 删除 companyName 字段。
2. PaymentPage.tsx：
   - 空态判断由 account === null 改为 !account.bound；空态视觉按设计稿 .design-refs/payout-empty.png（figma 15150-30625）1:1 核对：页头「收款管理/管理平台结算打款的对公收款账户信息」、大卡片居中灰色卡片图标+「尚未添加收款账户」+说明、内嵌白色小卡（黑色小图标+「添加收款账户」+「绑定对公银行账户，平台结算后直接打款到账。」+ 黑色通栏按钮「→ 立即添加」）。
   - 添加/编辑表单：「公司名称」改为只读 disabled 输入框（灰底），值 = 接口返回的 companyName，不参与提交；删除「户名 accountHolder」输入项；保留 银行账号*、开户银行*、支行名称(选填)；提交 body 不含 companyName。
   - DraftState 相应精简；编辑回填 accountNo/bank/branch。
3. EarningsPage.tsx：hasAccount 判断由 !!acc 改为 acc.bound。

【第二部分：收益明细字段补充（契约=收益明细-字段补充-20260701.md）】
接口变化：
- GET /publisher/settlement/overview 新增 settlementBillCount（结算账单数）；monthlyTrend 改为**本年 1—12 月固定 12 项、空月补 0、month 升序**，前端无需再补月。
- GET /publisher/settlement/earnings 新增入参 keyword（剧名模糊搜索，可与 courseId 并用）；每行新增 viewCount（该剧累计播放量，取 course.view_counts）、settleStatus（0待结算/1结算中/2已结算）。按月筛选用 startDate/endDate 传该月首末日。

改造点：
1. settlement.ts：EarningsSummary 加 settlementBillCount:number 并更新 monthlyTrend 注释；EarningsDetailRow 加 viewCount:number、settleStatus:0|1|2；EarningsDetailQuery 加 keyword?:string，getEarningsDetail 透传 keyword。
2. EarningsPage.tsx：
   - 总览统计区新增「结算账单」项，值 = settlementBillCount（无收款账户置灰逻辑与其他统计一致）。
   - 删除 normalizeTrend 及相关月份补齐函数，monthlyTrend 12 项直接映射进柱状图。
   - 数据加载重构为服务端驱动：月份 Tab 切换 → 以该月首末日为 startDate/endDate 重新请求；剧目下拉 → courseId 入参；搜索框 → keyword 入参（300ms 防抖）；删除 loadAllEarningsRows 全量拉取与前端 filter，改为常规分页（limit 10，表格底部分页控件，样式参照 src/app/distribution/pages/ContentPage.tsx 的分页写法）。月份 Tab 列表可先请求一次不带月份的明细取全部月份，或由「本年 12 个月中 monthlyTrend 金额>0 的月份 + 当前月」派生——选实现简单且交互合理的方案并在代码注释说明。
   - 明细表新增两列：「播放量」（viewCount，千分位格式化）和「结算状态」（0→待结算 灰徽标 / 1→结算中 蓝徽标 / 2→已结算 绿徽标，样式参照现有 TypeBadge / Badge 组件），列顺序：日期、剧目、类型、分成比例、订单数、播放量、结算状态、收益金额，注意扩 min-width 保持不换行。
   - growthRate === null 展示 "--"（已有逻辑，保留勿删）。
3. i18n（earnings.ts / payment.ts）四语言补：播放量、结算状态、待结算、结算中、已结算、结算账单等新 key。

【完成标准】
- pnpm build 通过、无新增 lint 错误；四语言文案齐全。
- 收款管理空态与 .design-refs/payout-empty.png 1:1；绑定态/表单态与现有风格一致且公司名只读。
- 收益明细：12 月柱状图、结算账单数、播放量列、结算状态徽标、服务端搜索/按月/分页全部可用。
- 不改动范围外文件（尤其不要动 WithdrawPage、ContentPage、member 相关）。
```

---

## 四、验收清单（发起人 review 用）

1. **构建**：`pnpm build` 通过，`pnpm lint`（若有）无新增报错。
2. **视觉 1:1**：本地起 dev server，逐状态截图与 `.design-refs/` 8 张原稿对照（布局/间距/圆角/徽标配色/文案/按钮样式）。
3. **成员管理**：非 11 位手机号前端拦截；403328/403329/403330 后端文案 toast；`removable=false` 灰置；`self` 显示「(我)」；角色卡数据来自接口非硬编码；banner 关闭后刷新不复现。
4. **收款管理**：`bound=false` 走空态且表单公司名自动带入只读；提交体不含 companyName；403315/403316 提示正常。
5. **收益明细**：柱状图恒 12 根；结算账单数展示；播放量为累计值（同剧多日重复属预期）；settleStatus 三态徽标；keyword 服务端搜索防抖；按月切换传首末日；`growthRate=null` 显示 "--"。
6. **i18n**：切换 zh-CN/zh-TW/en/pt 无缺 key、无中文硬编码残留。
7. **越权/异常**：非超管访问成员管理不白屏；token 失效（403343→401）跳登录。

## 五、已知范围外事项（待确认/后续迭代）

- **账号信息页面**（契约§3 已齐，设计稿未提供页面视觉）→ 本期不做，侧边栏不加入口。
- **员工剧目分配 UI**（契约§2）→ 无设计稿，本期不做。
- **updateRole 改角色 UI** → 设计稿列表无此交互，仅封装 service。
- 收益明细新版设计稿链接未在 `page-figma-design.md` 中给出 → 2b 按契约 + 现有页面风格实现，验收时以契约字段为准。
- 设计稿侧边栏出现「账号信息」「提现记录/结算记录」命名不一致 → 维持现有「结算记录」命名，「账号信息」随账号信息页一起做。
