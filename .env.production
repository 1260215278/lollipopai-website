# 生产构建环境变量
#
# 2026-09-05 改为**根部署**：生产环境 www.lollipop.im（Cloudflare）把 dist 放在网站根目录，
# 真实可索引 URL 是 https://www.lollipop.im/about，资源路径是 /assets/*.js。
#
# ⚠️ 这个值同时驱动三件事，必须与生产实际部署路径一致，否则整站崩坏：
#   1. Vite base → 所有资源引用（/assets/*.js、图片）
#   2. Router basename（getDeployBasename 读 import.meta.env.BASE_URL）→ 所有 <Link> 内链
#   3. 与 SITE_URL 常量（scripts/prerender-plugin.ts + src/app/i18n.seo.ts）必须配套
#
# 历史教训：这里曾设 VITE_BASE=/lollipop/，而生产是根部署 →
#   sitemap 里 298 条 URL 100% 被 SPA 兜底成首页壳 → GSC 525 个页面未编入索引。
# 若将来确实要部署到子路径，需同时改：本文件 + SITE_URL 常量 + 托管层 301 规则。
VITE_BASE=/
