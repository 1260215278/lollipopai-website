/**
 * content 组件共享类型：从 mock 数据模型与 PhoneMockup 复出，避免重复定义。
 */
export type {
  DramaRow,
  EpisodeRecord,
  DramaStatus,
  EpisodeStatus,
  DistributionType,
  RevenueType,
  CopyrightType,
  ChannelValue,
  CreateDramaInput,
} from "../../mock/content";

export type { Highlight } from "./PhoneMockup";
