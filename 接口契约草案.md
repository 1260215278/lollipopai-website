# 短剧发行中心 · 接口契约草案（交后端确认）

> 适用项目：`lollipopai-website` 发行中心后台。
> 现状：**发行者入驻已对接真接口**（见《发行者入驻-前端接口.md》，本文不重复）；**数据概览 / 上剧中心 / 结算中心接口尚未定义**，前端以 `VITE_USE_MOCK` 开关 mock 先行，字段语义反推自原型 `Lollipop短剧发行中心/src/app/components/dashboard/*` 与 figma 设计稿，**未在业务代码里编字段、未写撒网式 fallback**。本草案经后端确认锁定后，各 service 仅需把 `http` 真实分支路径/字段对齐、并设 `VITE_USE_MOCK=false` 即可切真，页面无需改动。
>
> **全局约定（沿用入驻接口）：**
> - context-path：`/sqx_fast`（由 `services/http.ts` 注入，下文路径不重复带）
> - 鉴权：请求头 `token`（取自 `services/auth.getToken()`）
> - 统一响应：`{ code, msg, data }`，`code === 0` 成功取 `data`；非 0 由前端 toast 后端已翻译好的 `msg`
> - 上传：封面 / 证照 / 剧集视频统一走已有 `POST /alioss/upload`（multipart，`file=`，`data` 为 URL 字符串），本文不重复
> - 数值口径：计数字段返回**原始整数**，金额返回以「元」为单位的 number，由前端按语言格式化（简中/繁中「万/亿」、英/葡「K/M」；金额展示 `¥ 1,234.50`）

---

## 0. 发行者入驻（已实现真接口）· 唯一待澄清项

入驻接口已按《发行者入驻-前端接口.md》实现并通过验收。落地中发现 **1 个需后端补充的字段**：

- **`GET /app/publisher/status` 缺少「账号是否已绑定手机号」的显式标识。**
  - 接口文档要求：账号已绑定手机号时，入驻表单手机号输入框应**只读锁定**为账号手机号；未绑定才可填。
  - 但 `/status` 响应仅有 `auditStatus / rejectReason / isPublisher / apply`，**无显式的「账号已绑定手机号」字段**。
  - 前端当前以 `apply.phone` 是否有值作为「已绑定」的近似信号（已申请过的用户回填并锁定）。**边界缺陷**：首次申请（`apply=null`）但账号实际已绑定手机号的用户，前端无法判定 → 会显示可填且必填，与后端「强制用账号手机号、忽略前端传值」语义不一致。
  - **建议**：`/status`（或账号资料接口）返回显式 `boundPhone: string | null`（或 `phoneBound: boolean` + 手机号），前端据此锁定，消除推断。

---

## 1. 数据概览（Overview）

字段语义反推自原型 `dashboard/HomePage.tsx` 与 figma `node-id=15077-19718`。前端落地：`services/dashboard.ts`、`mock/dashboard.ts`、`pages/OverviewPage.tsx`（强类型 `DashboardData/DashboardStats/TrendPoint/DramaRankItem`，零 `any`）。

### 1.1 获取数据概览（聚合）

一次性返回顶部统计卡 + 7/30 日趋势 + 剧集排行，避免首屏多次请求。

- **`GET /app/publisher/dashboard/overview`**　请求参数：无（范围由 token 决定）
- 响应 `data`：

```jsonc
{
  "stats": {
    "totalDramas":    { "value": 5,       "monthlyAdded": 2 },   // 上剧总数 + 本月新增部数
    "totalViews":     { "value": 3802000, "momPercent": 23.4 },  // 累计播放量 + 较上月环比%
    "totalLikes":     { "value": 138000,  "momPercent": 18.7 },  // 累计点赞数 + 较上月环比%
    "totalFavorites": { "value": 76000,   "momPercent": 15.2 }   // 累计收藏数 + 较上月环比%
  },
  "trend": {
    "7":  [ { "date": "6/10", "plays": 3480, "likes": 1150, "favs": 730 } /* 逐日 */ ],
    "30": [ { "date": "5/11", "plays": 800,  "likes": 200,  "favs": 120 } /* 采样 */ ]
  },
  "ranking": [
    { "rank": 1, "title": "星河恋人", "episodes": 24, "plays": 1482000,
      "likes": 32000, "favs": 18000, "comments": 4521, "shares": 9830, "trend": "up" }
    // 按 plays 降序
  ]
}
```

字段：`stats.*.value` 原始计数；`momPercent` 较上月环比（正=增长）；`trend["7"|"30"][]` 含 `date/plays/likes/favs`（前端可切周期与指标）；`ranking[]` 含 `rank/title/episodes/plays/likes/favs/comments/shares/trend("up"|"down")`。

### 1.2 待确认
1. 趋势是否仅 7/30 两档？是否需自定义日期范围（若需建议拆 `?period=` 或 `?start=&end=`）。
2. 近 30 天逐日还是采样？点数上限？
3. 排行口径：固定 Top 5 按播放降序，还是支持排序/分页？
4. `momPercent` 计算口径（自然月 vs 滚动 30 天）；无上月数据返回 `0` 还是 `null`？
5. 统计卡是否需更多维度（评论/分享/收益）？
6. 接受单接口聚合，还是拆 `stats/trend/ranking`？

---

## 2. 上剧中心（Content）

字段语义反推自原型 `dashboard/ContentPage.tsx` 与 figma 多 node。前端落地：`services/content.ts`、`mock/content.ts`、`pages/ContentPage.tsx` + `components/content/*`。

### 2.1 数据字典

**DramaRow（列表项 / 详情）**

| 字段 | 类型 | 语义 |
|---|---|---|
| `id` | string | 短剧 ID（mock 形如 `D20260105001`） |
| `name` / `description` | string | 名称 / 简介（≤200 字） |
| `cover` | string | 封面 URL（空串=未上传） |
| `episodes` / `uploadedEpisodes` | number | 总集数 / 已上传集数 |
| `countries` | string[] | 上架国家 value，枚举 `us`/`ph`/`in` |
| `tags` | string[] | 标签 value（见枚举） |
| `distribution` | string | `account`(账号主页) / `full`(全量推荐) |
| `status` | string | `online`/`offline`/`reviewing`/`not_published` |
| `copyright` | string | `self`(自制) / `licensed`(授权) |
| `revenueType` | string | `account`(订阅 2:8) / `full`(订阅 4:6) |
| `channel` | string | `male`/`female`/`general` |
| `uploadedAt` | string | `YYYY-MM-DD` |

> 约束：`distribution=full ⇒ revenueType=full`（平台4:出品方6）；`account ⇒ account`（2:8）。
> 标签枚举：`urban|rural|romance|youth|family|mystery|comeback|plot|wuxia|comedy|ancient|campus`。

**EpisodeRecord（单集明细）**：`ep`(序号) / `title` / `duration`(`m:ss`，未知 `—`) / `size`(展示串，未知 `—`) / `uploadedAt` / `status`(`uploaded`/`processing`/`failed`)。

### 2.2 接口列表

| 用途 | Method / Path | 入参 | `data` |
|---|---|---|---|
| 短剧列表 | `GET /app/drama/list` | `keyword?`（名称/ID 模糊） | `DramaRow[]` |
| 短剧详情 | `GET /app/drama/detail` | `id` | `DramaRow` |
| 剧集明细 | `GET /app/drama/episodes` | `dramaId` | `EpisodeRecord[]` |
| 创建短剧 | `POST /app/drama/create` | 见下 | 新建 `DramaRow` |
| 上/下架 | `POST /app/drama/status` | `{ id, status: "online"\|"offline" }` | `DramaRow` |
| 提交剧集元数据 | `POST /app/drama/episodes/submit` | `{ dramaId, episodes:{ ep, videoUrl, size, duration }[] }` | `EpisodeRecord[]` |

**创建短剧 body**：`name` / `description` / `cover`(URL) / `countries[]` / `tags[]` / `totalEpisodes` / `copyrightType`(`self`\|`licensed`) / `channel` / `distribution`(`account`\|`full`) / `publishStatus`(`online`→进入审核 / `offline`→未上架)。
业务规则：`publishStatus=online ⇒ status=reviewing`；`offline ⇒ not_published`；`reviewing` 不可上下架（前端已禁用入口）。

### 2.3 前端写死常量（国家收费规则，US/PH/IN，只读不入接口）

> 硬约束：前端写死、原样移植原型、数值不可改；仅展示，不参与提交。出处 `mock/content.ts` 的 `COUNTRIES`。

| 国家(value) | 整部剧 | 单集 | 15s 广告费 |
|---|---|---|---|
| 美国 (`us`) 🇺🇸 | $80 | $10 | 无 |
| 菲律宾 (`ph`) 🇵🇭 | $12 | $1.5 | $0.008/次 |
| 印度 (`in`) 🇮🇳 | $8 | $0.8 | $0.005/次 |

### 2.4 待确认
1. 列表是否分页/排序？（mock 全量 + 前端按 keyword 过滤）
2. 创建短剧时剧集视频随创建提交元数据，还是创建后单独提交？（前端按后者，见 episodes/submit）
3. 审核状态机是否需前端轮询/推送？（当前 `reviewing` 为提交后即时态，无轮询）
4. `size`/`duration` 前端上报还是后端转码回填？（建议后端回填为准）
5. **上架维度（国家 vs 语言）**：figma 在列表/发布配置局部出现「上架语言（英语/葡萄牙语/中文）」措辞，但收费值与详情/下架页一致采用国家（US/PH/IN）。**figma 自身不一致**，本期按硬约束统一建模为**国家**（US/PH/IN，价格原样）。若产品最终以语言维度为准，需对齐 `countries` 字段语义（价格值不变）。**请产品/后端拍板。**

---

## 3. 结算中心（Settlement）

字段语义反推自原型 `dashboard/{PaymentPage,EarningsPage,WithdrawPage}.tsx` 与 figma `15150-*`/`15151-*`/`15135-27153`/`15188-33359`。前端落地：`services/settlement.ts`、`mock/settlement.ts`、`pages/{Payment,Earnings,Withdraw}Page.tsx` + `components/settlement/*`。

### 3.1 收款管理（对公收款账户）

| 用途 | Method / Path | 入参 / body | `data` |
|---|---|---|---|
| 获取已绑定账户 | `GET /app/publisher/payout/account` | 无 | `PayoutAccount \| null` |
| 公司名称（自动带入） | `GET /app/publisher/payout/company` | 无 | `string` |
| 新增账户 | `POST /app/publisher/payout/account` | `PayoutAccountBody` | `PayoutAccount` |
| 修改账户 | `POST /app/publisher/payout/account/update` | `PayoutAccountBody` | `PayoutAccount` |

- `PayoutAccount`：`{ companyName, accountNo, bank, branch? }`
- `PayoutAccountBody`：`{ accountNo(必), bank(必), branch? }`
- figma 仅「修改」无「删除」，故未提供删除接口。

### 3.2 收益明细

- **`GET /app/publisher/earnings/summary`** → `EarningsSummary`：`totalCumulative`(累计) / `withdrawable`(可提现) / `processing`(结算中) / `withdrawn`(已提现) / `relatedDramas` / `estThisMonth`(本月预估) / `bills`(账单数) / `growthRate`(环比%) / `monthlyTrend: { month:1-12, total }[]`。
- **`GET /app/publisher/earnings/detail`**　query `month?`(`yyyy-MM`，不传返回全部，前端据全部派生月份 Tab) → `EarningsDetailRow[]`：`{ month, drama, type("full"|"account"), ratio, views(展示串), amount, status("settled"|"processing"|"pending") }`。

### 3.3 结算记录

- **`GET /app/publisher/settlement/records`** → `SettlementRecord[]`：`{ id, date, period(yyyy-MM), ratio("4：6" 全角冒号), type("full"|"account"), accountType("cn"|"overseas"), accountNo(脱敏尾号), amount, status("unpaid"|"paid") }`（前端做状态筛选 + 周期/类型搜索）。
- **`POST /app/publisher/settlement/apply`**　body `{ recordId }` → 成功 `code===0`（行内「申请结算」，`unpaid` 可点、`paid` 禁用）。

### 3.4 枚举汇总

| 字段 | 枚举 | 含义 |
|---|---|---|
| `type` | `full`/`account` | 全量推荐订阅 / 账户主页订阅 |
| 收益 `status` | `settled`/`processing`/`pending` | 已结算 / 结算中 / 待结算 |
| `accountType` | `cn`/`overseas` | 中国公户 / 海外公户 |
| 结算 `status` | `unpaid`/`paid` | 未打款 / 已打款 |

### 3.5 待确认
1. 公司名称随 3.1 返回，还是单独 3.1「company」接口？
2. 收款账户是否支持删除？（figma 仅「修改」）
3. **结算申请成功后的状态机**：是否有「申请中/审核中」中间态？mock 暂乐观置 `paid`。若有中间态请给枚举+文案，前端补徽章色与 i18n。
4. 账户号码脱敏：后端返回脱敏串（如 `****4567`）还是前端脱敏？本期按后端返回脱敏串。
5. 金额单位/精度：当前「元 + 两位小数」；多币种需补币种字段。

---

> 演示链路说明：结算中心 mock 默认无收款账户（先看「未添加」空态）；在「收款管理」添加账户后，同会话内「收益明细 / 结算记录」展示数据态（mock 内存存储，接真后走 http 分支自动失效）。
