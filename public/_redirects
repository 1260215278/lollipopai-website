# Cloudflare Pages 重定向规则（2026-09-05 根部署迁移）
#
# 背景：生产环境 www.lollipop.im 把 dist 部署在网站根目录，但历史上构建产物里
# canonical / sitemap / hreflang / JSON-LD 全部写死了 /lollipop/ 前缀，导致
# sitemap 里 298 条 URL 100% 被 SPA 兜底成首页壳 → GSC 525 个页面未编入索引
# （398 自动重定向 + 103 备用网页 + 17 noindex + 3 403 + 1 重复 + 3 待抓取）。
#
# 现已把全站前缀剥离为根部署，这里把已被引擎收录的 /lollipop/... 旧 URL 301 到
# 根路径，把权重传给新 URL，并让 GSC 的「自动重定向」计数自然消化。
#
# ⚠️ 语法：/源路径   /目标路径  状态码
#    :splat 表示源路径里 * 匹配到的部分
#    顺序敏感 —— 更具体的规则必须写在前面

/lollipop/*  /:splat  301
/lollipop    /        301
